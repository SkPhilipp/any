#!/usr/bin/env bash

set -e
set -o pipefail

command=$1

if [ -z "$command" ]; then
  echo "Usage: any <command>" >&2
  echo "  Commands:" >&2
  echo "    build" >&2
  exit 1
fi

if [ "$command" = "build" ]; then
  shift
  any-build "$@"
else
  echo "Unknown command: $command" >&2
  exit 1
fi
