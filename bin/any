#!/usr/bin/env bash

set -e
set -o pipefail

command=$1

if [ -z "$command" ]; then
  echo "Usage: $0 <command>" >&2
  exit 1
fi

if [ "$command" = "build" ]; then
  shift
  any-build "$@"
else
  echo "Unknown command: $command" >&2
  exit 1
fi
